{# Owned text preview triggers: enable edit action from the global media preview modal. #}
data-media-preview-text-editable="1"
data-files-row-id="{{ file.id }}"
data-files-row-name="{{ file.originalFileName }}"
data-files-row-ext="{{ fileExt }}"
