{{ plainBlocks.title|default('mail.password_reset.title'|trans({}, 'messages', locale|default('en'))) }}

{{ plainBlocks.intro|default('mail.password_reset.intro'|trans({}, 'messages', locale|default('en'))) }}

{{ labels.cta|default('mail.password_reset.cta'|trans({}, 'messages', locale|default('en'))) }}: {{ resetUrl }}

{{ plainBlocks.expiry_hint|default('mail.password_reset.expiry_hint'|trans({}, 'messages', locale|default('en'))) }}
{{ plainBlocks.security_hint|default('mail.password_reset.security_hint'|trans({}, 'messages', locale|default('en'))) }}

{{ plainBlocks.footer|default('mail.password_reset.footer'|trans({}, 'messages', locale|default('en'))) }}
