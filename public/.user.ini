; PHP-FPM / CGI (shared hosting): per-request limits for chunked uploads (~8 MB chunks).
; Total file size is assembled server-side and is not limited by these values.
upload_max_filesize = 32M
post_max_size = 36M
max_execution_time = 3600
max_input_time = 3600
memory_limit = 1024M
